from setuptools import setup, find_packages

setup(
    name='stubs',
    version='0.1',
    packages=find_packages(),
    namespace_packages=['ni'],
    description='Test class stubs for local unit tests',
)
